cp -vn "$1" "$2"/"$(md5sum "$1" | cut -f 1 -d ' ')"~"${1##*/}"
#folder_name=basename `dirname $1`
#install -D "$1" "$2"/$folder_name/"$(md5sum "$1" | cut -f 1 -d ' ')"~"${1##*/}"

#cp -vn "$1" "$2"/ || cp -vn "$1" "$2"/"${1##*/}"~"$(md5sum "$1" | cut -f1 -d' ')"

#MD5=$(md5 "$file" | awk '{print $1}')
#cp "$file" $(echo "$file" | sed -e "s/.mov/_${MD5}.mov/")
